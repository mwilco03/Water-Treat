# cmdline.txt - Kernel command line for Raspberry Pi
#
# To enable firstrun.sh on Raspberry Pi OS:
# 1. Copy firstrun.sh to /boot/firmware/firstrun.sh
# 2. Append to your existing /boot/firmware/cmdline.txt:
#
#    systemd.run=/boot/firmware/firstrun.sh systemd.run_success_action=reboot
#
# IMPORTANT: cmdline.txt must be ONE LINE. Append to existing, don't replace.
#
# Example full cmdline.txt (your values may differ):
# console=serial0,115200 console=tty1 root=PARTUUID=xxxxx rootfstype=ext4 fsck.repair=yes rootwait systemd.run=/boot/firmware/firstrun.sh systemd.run_success_action=reboot
